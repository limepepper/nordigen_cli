---

name: nordigen_cli

services:
  web:
    image: python312_pip
    build:
        context: ..
        dockerfile: .docker/python312_pip/Dockerfile
    # command: gunicorn -b 0.0.0.0:5000 myapi.wsgi:app
#    env_file:
#      - ./.flaskenv
#    environment:
#      - DATABASE_URI=sqlite:////db/myapi.db
#      - CELERY_BROKER_URL=amqp://guest:guest@rabbitmq
#      - CELERY_RESULT_BACKEND_URL=redis://redis
#    volumes:
#      - ../src/myapi:/code/myapi:zro
#      - ./db/:/db/:z
#    ports:
#      - "5000:5000"
